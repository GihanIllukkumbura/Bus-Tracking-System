<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Vehicle</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .container {
      max-width: 600px;
      margin-top: 50px;
    }
    .form-label {
      font-weight: bold;
    }
    .error {
      color: red;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>

  <div class="container">
    <h1 class="text-center mb-4">Edit Vehicle</h1>
    
    <% if (error) { %>
      <div class="alert alert-danger" role="alert"><%= error %></div>
    <% } %>

    <form action="/update/<%= vehicleId %>" method="post" enctype="multipart/form-data">
      <div class="mb-3">
        <label for="sltbnumber" class="form-label">SLTB Number:</label>
        <input type="text" class="form-control" id="sltbnumber" name="sltbnumber" value="<%= vehicle.sltbnumber %>" required>
      </div>

      <div class="mb-3">
        <label for="model" class="form-label">Model:</label>
        <input type="text" class="form-control" id="model" name="model" value="<%= vehicle.model %>" required>
      </div>

      <div class="mb-3">
        <label for="year" class="form-label">Year:</label>
        <input type="number" class="form-control" id="year" name="year" value="<%= vehicle.year %>" required>
      </div>

      <div class="mb-3">
        <label for="registeredNumber" class="form-label">Registered Number:</label>
        <input type="text" class="form-control" id="registeredNumber" name="registeredNumber" value="<%= vehicle.registeredNumber %>" required>
      </div>

      <div class="mb-3">
        <label for="fuelCapacity" class="form-label">Fuel Capacity (liters):</label>
        <input type="number" class="form-control" id="fuelCapacity" name="fuelCapacity" value="<%= vehicle.fuelCapacity %>" required>
      </div>

      <div class="mb-3">
        <label for="fuelType" class="form-label">Fuel Type:</label>
        <input type="text" class="form-control" id="fuelType" name="fuelType" value="<%= vehicle.fuelType %>" required>
      </div>

      <div class="mb-3">
        <label for="vehicalImage" class="form-label">Vehicle Image:</label>
        <input type="file" class="form-control" id="vehicalImage" name="vehicalImage" accept="image/*">
      </div>

      <div class="d-grid">
        <button type="submit" class="btn btn-primary">Update Vehicle</button>
      </div>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
